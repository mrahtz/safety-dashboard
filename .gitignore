__pycache__/
*.pyc
.pytest_cache/
*.egg-info/
.venv/
# Run-time artifacts (frozen blobs, crops, db, static export) live under var/.
# Regenerate with scripts/ingest.sh + scripts/export.sh; never commit them.
/var/
# Downloaded source cache (content-addressed PDFs etc., via fetch.local_copy).
/cache/
*.sqlite
*.db
*.zip
